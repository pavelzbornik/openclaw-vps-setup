# Example Terraform variables file
# Copy this to terraform.tfvars and fill in your actual values
# NEVER commit terraform.tfvars to version control!

# 1Password vault name where Discord secrets are stored
onepassword_vault = "Infrastructure"

# 1Password item name containing Discord credentials
# This item should have fields:
# - username: bot_token (with your Discord bot token)
# - password or text field: server_id (with your Discord server ID)
onepassword_discord_item = "Discord OpenClaw Bot"
