---
# vault.yml is generated by scripts/bootstrap-1password.ps1 — do not edit manually.
# Values are read from 1Password items in the OpenClaw vault.
# Run bootstrap-1password.ps1 to create/update vault.yml.
#
# This reference shows the expected structure and 1Password item sources.

# Discord user IDs allowed to DM the bot (wired to openclaw_discord_allowlist in all.yml)
vault_openclaw_discord_allowlist:
  - "REPLACE_ME_USER_ID"

# Discord guild IDs the bot is active in (wired to openclaw_discord_guilds in all.yml)
vault_openclaw_discord_guilds:
  - "REPLACE_ME_GUILD_ID"

# IDENTITY.md content for agent workspace bootstrap (never overwritten after first deploy)
vault_openclaw_identity_md: |
  # IDENTITY.md
  <!-- Replace with agent identity content -->

# USER.md content for agent workspace bootstrap (never overwritten after first deploy)
vault_openclaw_user_md: |
  # USER.md
  <!-- Replace with user context content -->
