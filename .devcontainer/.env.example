# Loaded by ansible-control via env_file in docker-compose.yml.

# 1Password Service Account token — used by Ansible op inject
# Create one: https://developer.1password.com/docs/service-accounts/
OP_SERVICE_ACCOUNT_TOKEN=ops_...

# Optional: uncomment to set fixed host ports (default 0 = random)
# OPENCLAW_SSH_PORT=2222
# OPENCLAW_GATEWAY_PORT=3000
