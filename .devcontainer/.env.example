# Copy this file to .env (gitignored) and fill in your values.
# Docker Compose reads .env automatically from its directory.

# 1Password Service Account token — used by Ansible op inject
# Create one: https://developer.1password.com/docs/service-accounts/
OP_SERVICE_ACCOUNT_TOKEN=ops_...

# Optional: set fixed host ports to avoid surprises (0 = random)
OPENCLAW_SSH_PORT=0
OPENCLAW_GATEWAY_PORT=0
